import React from "react";
import ReactDOM from "react-dom";
import App from "./App";



const roott = ReactDOM.createRoot(document.querySelector("#root"));

roott.render(
  <React.StrictMode>
   <App/>
  </React.StrictMode>
);
